class Processor {
  /**
  input array
  **/
  constructor(config) {
    this.config = config;
  }

  process(input) {}

  // request() {}
  // requestMultiple() {}
  //
  // map(data) {}
  // persist(data) {}
  // generateOutput() {}
}

module.exports = Processor;
